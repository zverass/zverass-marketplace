# ZVERASS Marketplace

Полнофункциональный маркетплейс с автоматической выдачей товаров, системой рейтингов, чатом и админ-панелью.

## 🚀 Особенности

✅ **Авторизация и роли** (Покупатель, Продавец, Админ)
✅ **Каталог товаров** с категориями и поиском
✅ **Автоматическая выдача ключей/файлов**
✅ **Система заказов** с подтверждением платежа
✅ **Баланс продавца** и запросы на вывод
✅ **Отзывы и рейтинги**
✅ **Промокоды** (управление админом)
✅ **Чат** для общения между пользователями
✅ **Админ-панель** для модерации

## 📋 Требования

- Node.js 14+
- PostgreSQL 12+
- npm или yarn

## 🔧 Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/yourusername/zverass-marketplace.git
cd zverass-marketplace
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Настройка переменных окружения

Скопируйте `.env.example` в `.env` и заполните данные:

```bash
cp .env.example .env
```

Отредактируйте `.env`:

```env
NODE_ENV=development
PORT=3000

# Database
DB_HOST=localhost
DB_PORT=5432
DB_USER=zverass
DB_PASSWORD=your_password
DB_NAME=zverass_marketplace

# JWT
JWT_SECRET=your_secret_key_here
JWT_EXPIRE=7d

# Support
SUPPORT_PHONE=+7 906 829-34-10
SUPPORT_TELEGRAM=@zverass_support
```

### 4. Создание базы данных PostgreSQL

```bash
# Создайте БД (БД автоматически инициализируется при первом запуске)
createdb zverass_marketplace
```

### 5. Запуск в разработке

```bash
npm run dev
```

Приложение будет доступно на `http://localhost:3000`

### 6. Запуск в продакшене

```bash
npm start
```

## 📁 Структура проекта

```
/zverass-marketplace
├── server.js              # Главный файл приложения
├── package.json
├── .env                   # Переменные окружения
├── /config
│   └── database.js        # Подключение к БД и инициализация схемы
├── /models                # Модели данных
│   ├── User.js
│   ├── Product.js
│   ├── Order.js
│   ├── Review.js
│   ├── PromoCode.js
│   ├── ProductKey.js
│   ├── Chat.js
│   └── Withdrawal.js
├── /controllers           # Логика обработки запросов
│   ├── authController.js
│   ├── productController.js
│   ├── orderController.js
│   ├── sellerController.js
│   ├── chatController.js
│   └── adminController.js
├── /routes                # Роуты API
│   ├── auth.js
│   ├── products.js
│   ├── orders.js
│   ├── seller.js
│   ├── chat.js
│   └── admin.js
├── /middleware            # Middleware
│   ├── auth.js
│   ├── roles.js
│   └── upload.js
├── /utils                 # Вспомогательные функции
│   ├── generateToken.js
│   └── helpers.js
└── /public                # Статические файлы
    ├── index.html
    ├── catalog.html
    ├── login.html
    ├── register.html
    ├── /profile
    ├── /seller
    ├── /admin
    ├── /css
    │   └── style.css
    ├── /js
    │   ├── api.js
    │   └── main.js
    └── /uploads
```

## 🔌 API Endpoints

### Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `POST /api/auth/logout` - Выход
- `GET /api/auth/me` - Текущий пользователь
- `PUT /api/auth/profile` - Обновление профиля

### Товары
- `GET /api/products` - Все товары
- `GET /api/products/:id` - Один товар
- `POST /api/products` - Создать товар (продавец)
- `PUT /api/products/:id` - Обновить товар (продавец)
- `DELETE /api/products/:id` - Удалить товар (продавец)
- `GET /api/products/best-selling` - Лучшие продажи
- `POST /api/products/:id/keys` - Добавить ключи (продавец)

### Заказы
- `POST /api/orders` - Создать заказ
- `GET /api/orders` - Мои заказы
- `GET /api/orders/:orderNumber` - Один заказ
- `POST /api/orders/:orderNumber/confirm-payment` - Подтвердить платеж
- `POST /api/orders/:orderNumber/review` - Оставить отзыв

### Продавец
- `GET /api/seller/dashboard` - Дашборд
- `GET /api/seller/balance` - Баланс
- `POST /api/seller/withdrawal` - Запрос вывода
- `GET /api/seller/sales` - История продаж

### Чат
- `POST /api/chat/send` - Отправить сообщение
- `GET /api/chat/messages` - Получить сообщения
- `GET /api/chat/dialogs` - Список диалогов
- `DELETE /api/chat/dialogs/:otherUserId` - Удалить диалог

### Админ
- `GET /api/admin/dashboard` - Дашборд
- `GET /api/admin/products/pending` - Товары на модерации
- `POST /api/admin/products/:productId/approve` - Одобрить товар
- `POST /api/admin/products/:productId/reject` - Отклонить товар
- `GET /api/admin/withdrawals/pending` - Выводы на одобрение
- `POST /api/admin/withdrawals/:withdrawalId/approve` - Одобрить вывод
- `POST /api/admin/promo-codes` - Создать промокод
- `GET /api/admin/promo-codes` - Список промокодов

## 🚀 Деплой на Render

### 1. Подготовка к деплою

```bash
# Убедитесь, что все изменения закоммичены
git add .
git commit -m "Prepare for deployment"
git push origin main
```

### 2. Создание сервиса на Render

1. Перейдите на [render.com](https://render.com)
2. Создайте новый Web Service
3. Подключите репозиторий GitHub
4. Выберите главную ветку
5. Установите окружение на Node.js

### 3. Настройка переменных окружения

В Render dashboard установите следующие переменные:

```
NODE_ENV=production
PORT=3000
DB_HOST=your_postgres_host
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=zverass_marketplace
JWT_SECRET=your_long_secret_key_here
SUPPORT_PHONE=+7 906 829-34-10
SUPPORT_TELEGRAM=@zverass_support
```

### 4. Команды запуска

Build command:
```
npm install
```

Start command:
```
npm start
```

### 5. Создание PostgreSQL базы

1. Создайте PostgreSQL service в Render
2. Скопируйте connection string
3. Используйте этот connection string для переменных окружения

### 6. Деплой

Render автоматически задеплоит приложение после каждого push в main branch.

## 🔑 Тестовые учетные данные

После запуска приложение автоматически создает все необходимые таблицы и категории.

### Создание тестовых пользователей:

1. Регистрация покупателя: `/register` → выбрать "Покупатель"
2. Регистрация продавца: `/register` → выбрать "Продавец"

### Создание админа (вручную в БД):

```sql
-- В PostgreSQL создайте администратора
INSERT INTO users (username, email, password, role) 
VALUES ('admin', 'admin@zverass.com', 'bcrypt_hashed_password', 'admin');
```

## 💳 Система платежей

Приложение имеет кнопку "Я оплатил" с 20-секундным таймером. В реальной версии это должно быть интегрировано с платежным шлюзом (PayPal, Stripe и т.д.).

## 📊 Комиссия платформы

- Продавец получает: **85%** от суммы заказа
- Платформа берет: **15%** от суммы заказа

## 🛡️ Безопасность

- JWT токены с httpOnly cookies
- Bcrypt для хеширования паролей
- CORS защита
- SQL injection protection через параметризованные запросы
- XSS защита через escape функции

## 📝 Логирование

Все ошибки логируются в консоль и файлы логов (при использовании Winston).

## 🤝 Поддержка

Для поддержки используется внутренний чат или контакты:
- Телефон: {{SUPPORT_PHONE}}
- Telegram: {{SUPPORT_TELEGRAM}}

## 📄 Лицензия

MIT License - Используйте свободно

## 🎯 Будущие улучшения

- [ ] WebSocket для чата вместо polling
- [ ] Интеграция с платежными шлюзами
- [ ] Email уведомления
- [ ] Двухфакторная аутентификация
- [ ] Система вотчлиста
- [ ] Фильтры и сортировка товаров
- [ ] API документация (Swagger)
- [ ] Логирование (Winston)
- [ ] Кэширование (Redis)
- [ ] CDN для изображений
